# archiguide-ui-integration

Use this skill when wiring UI to the local architecture dataset, filters, routes, and maps.

## Focus

- keep route behavior predictable
- preserve source-of-truth data in `lib/site-data.ts`
- ensure counts, filters, and links stay consistent across pages
- do not introduce CMS or backend assumptions unless requested
